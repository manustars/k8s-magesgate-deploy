apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-samba-state
  namespace: share
spec:   # stesso storageclass
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi